platform: linux

image_resource:
  type: docker-image
  source: {repository: busybox}

inputs:
  - name: concourse
outputs: 
  - name: source

run:
  # dir: concourse
  path: sh
  args:
  - -exc
  - |
    DIR="${PWD}"
    ls
    cp -R ./concourse ./source
