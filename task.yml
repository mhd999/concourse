platform: linux

image_resource:
  type: docker-image
  source: {repository: busybox}

inputs:
  - name: concourse

run:
  # dir: concourse
  path: sh
  args:
  - -exc
  - |
    cat ./concourse/schema.graphql
